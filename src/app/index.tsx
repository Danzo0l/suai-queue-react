import '@src/app/styles/index.scss';
import { withProviders } from './providers';

// eslint-disable-next-line react-refresh/only-export-components
const App = () => {
  return <div className="app h-full"></div>;
};

// eslint-disable-next-line react-refresh/only-export-components
export default withProviders(App);
